Drop the `@a16n/pact-server` tarball here as `a16n-pact-server.tgz` (the
filename `package.json` references — keeping it version-less means upgrades
never touch `package.json`):

```bash
cp path/to/pact/artifacts/a16n-pact-server-<version>.tgz vendor/a16n-pact-server.tgz
```
